apiVersion: v1
kind: ServiceAccount
metadata:
  name: watermark-app
  namespace: watermark
  annotations:
    # Substituted by CI's update-gitops job — see .github/workflows/ci.yaml
    eks.amazonaws.com/role-arn: __WATERMARK_APP_IRSA_ROLE_ARN__
