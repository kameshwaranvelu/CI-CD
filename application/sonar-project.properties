# SonarCloud keys — verify these match YOUR project in SonarCloud
# (SonarCloud > your project > Administration > Update Key).
# The default pattern for a personal account is <login>_<repo> and <login>.
sonar.projectKey=kameshwaranvelu_watermark-app
sonar.organization=kameshwaranvelu

sonar.sources=.
sonar.exclusions=**/tests/**,**/test_*.py,**/venv/**
sonar.tests=.
sonar.test.inclusions=test_*.py

sonar.python.coverage.reportPaths=coverage.xml
sonar.python.version=3.12
