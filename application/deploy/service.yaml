apiVersion: v1
kind: Service
metadata:
  name: watermark-app
  namespace: watermark
  labels:
    app: watermark-app
spec:
  selector:
    app: watermark-app
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
